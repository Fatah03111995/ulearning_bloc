import 'package:flutter/material.dart';

class YourCourse extends StatelessWidget {
  const YourCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Your Course'),
      ),
    );
  }
}
